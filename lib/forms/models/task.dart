import 'package:shared_pref/forms/helpers/db_helper.dart';

class TaskModel {
  // int id;
  // String name;
  // bool isComplete;
  //
  // TaskModel({this.id, this.name, this.isComplete});
  //
  // TaskModel.fromMap(Map map) {
  //   this.id = map[DbHelper.idColumnName];
  //   this.name = map[DbHelper.nameColumnName];
  //   this.isComplete = map[DbHelper.isCompleteColumnName] == 1 ? true : false;
  // }
  //
  // toMap() {
  //   return {
  //     DbHelper.nameColumnName: this.name,
  //     DbHelper.isCompleteColumnName: this.isComplete ? 1 : 0,
  //   };
  // }
}
